import { c as createLucideIcon, r as reactExports, u as useComposedRefs, a as useDirection, j as jsxRuntimeExports, P as Primitive, b as Presence, d as createContextScope, e as composeEventHandlers, f as useCallbackRef, g as useLayoutEffect2, h as cn, D as Database, L as Layers, C as COLUMN_ALIASES, n as normalizeHeader, U as Upload, X, B as Button, i as useEngineStore } from "./index-D4QMwWvE.js";
import { c as clamp, B as Badge, S as Switch, C as Collapsible, a as CollapsibleTrigger, b as CollapsibleContent, d as Search, I as Input } from "./input-C2561uCf.js";
import { T as Table, a as TableHeader, b as TableRow, c as TableHead, d as TableBody, e as TableCell, C as ChevronDown, B as BarChart, X as XAxis, Y as YAxis, f as Tooltip, g as Bar, h as Cell } from "./BarChart-DqfCYvDs.js";
import { E as EmptyState } from "./EmptyState-BitJLYM5.js";
import { C as ChevronRight, R as ResponsiveContainer, M as Minus } from "./ResponsiveContainer-D8JxM1vG.js";
import { P as Progress } from "./progress-qd8NrHmn.js";
import { S as Sparkles } from "./sparkles-B6YI30ep.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$a = [
  ["path", { d: "M8 2v4", key: "1cmpym" }],
  ["path", { d: "M16 2v4", key: "4m81vk" }],
  ["rect", { width: "18", height: "18", x: "3", y: "4", rx: "2", key: "1hopcy" }],
  ["path", { d: "M3 10h18", key: "8toen8" }]
];
const Calendar = createLucideIcon("calendar", __iconNode$a);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$9 = [
  ["path", { d: "M3 3v16a2 2 0 0 0 2 2h16", key: "c24i48" }],
  ["path", { d: "M18 17V9", key: "2bz60n" }],
  ["path", { d: "M13 17V5", key: "1frdt8" }],
  ["path", { d: "M8 17v-3", key: "17ska0" }]
];
const ChartColumn = createLucideIcon("chart-column", __iconNode$9);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$8 = [
  ["line", { x1: "4", x2: "20", y1: "9", y2: "9", key: "4lhtct" }],
  ["line", { x1: "4", x2: "20", y1: "15", y2: "15", key: "vyu0kd" }],
  ["line", { x1: "10", x2: "8", y1: "3", y2: "21", key: "1ggp8o" }],
  ["line", { x1: "16", x2: "14", y1: "3", y2: "21", key: "weycgp" }]
];
const Hash = createLucideIcon("hash", __iconNode$8);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$7 = [
  ["path", { d: "M21 12h-8", key: "1bmf0i" }],
  ["path", { d: "M21 6H8", key: "1pqkrb" }],
  ["path", { d: "M21 18h-8", key: "1tm79t" }],
  ["path", { d: "M3 6v4c0 1.1.9 2 2 2h3", key: "1ywdgy" }],
  ["path", { d: "M3 10v6c0 1.1.9 2 2 2h3", key: "2wc746" }]
];
const ListTree = createLucideIcon("list-tree", __iconNode$7);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  [
    "path",
    {
      d: "M11 21.73a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73z",
      key: "1a0edw"
    }
  ],
  ["path", { d: "M12 22V12", key: "d0xqtd" }],
  ["polyline", { points: "3.29 7 12 12 20.71 7", key: "ousv84" }],
  ["path", { d: "m7.5 4.27 9 5.15", key: "1c824w" }]
];
const Package = createLucideIcon("package", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["path", { d: "M5 12h14", key: "1ays0h" }],
  ["path", { d: "M12 5v14", key: "s699le" }]
];
const Plus = createLucideIcon("plus", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2", key: "afitv7" }],
  ["path", { d: "M21 9H3", key: "1338ky" }],
  ["path", { d: "M21 15H3", key: "9uk58r" }]
];
const Rows3 = createLucideIcon("rows-3", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  [
    "path",
    {
      d: "M18 7V5a1 1 0 0 0-1-1H6.5a.5.5 0 0 0-.4.8l4.5 6a2 2 0 0 1 0 2.4l-4.5 6a.5.5 0 0 0 .4.8H17a1 1 0 0 0 1-1v-2",
      key: "wuwx1p"
    }
  ]
];
const Sigma = createLucideIcon("sigma", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["circle", { cx: "9", cy: "12", r: "3", key: "u3jwor" }],
  ["rect", { width: "20", height: "14", x: "2", y: "5", rx: "7", key: "g7kal2" }]
];
const ToggleLeft = createLucideIcon("toggle-left", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["circle", { cx: "15", cy: "12", r: "3", key: "1afu0r" }],
  ["rect", { width: "20", height: "14", x: "2", y: "5", rx: "7", key: "g7kal2" }]
];
const ToggleRight = createLucideIcon("toggle-right", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  [
    "path",
    {
      d: "M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z",
      key: "1xq2db"
    }
  ]
];
const Zap = createLucideIcon("zap", __iconNode);
function useStateMachine(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var SCROLL_AREA_NAME = "ScrollArea";
var [createScrollAreaContext] = createContextScope(SCROLL_AREA_NAME);
var [ScrollAreaProvider, useScrollAreaContext] = createScrollAreaContext(SCROLL_AREA_NAME);
var ScrollArea$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeScrollArea,
      type = "hover",
      dir,
      scrollHideDelay = 600,
      ...scrollAreaProps
    } = props;
    const [scrollArea, setScrollArea] = reactExports.useState(null);
    const [viewport, setViewport] = reactExports.useState(null);
    const [content, setContent] = reactExports.useState(null);
    const [scrollbarX, setScrollbarX] = reactExports.useState(null);
    const [scrollbarY, setScrollbarY] = reactExports.useState(null);
    const [cornerWidth, setCornerWidth] = reactExports.useState(0);
    const [cornerHeight, setCornerHeight] = reactExports.useState(0);
    const [scrollbarXEnabled, setScrollbarXEnabled] = reactExports.useState(false);
    const [scrollbarYEnabled, setScrollbarYEnabled] = reactExports.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setScrollArea(node));
    const direction = useDirection(dir);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaProvider,
      {
        scope: __scopeScrollArea,
        type,
        dir: direction,
        scrollHideDelay,
        scrollArea,
        viewport,
        onViewportChange: setViewport,
        content,
        onContentChange: setContent,
        scrollbarX,
        onScrollbarXChange: setScrollbarX,
        scrollbarXEnabled,
        onScrollbarXEnabledChange: setScrollbarXEnabled,
        scrollbarY,
        onScrollbarYChange: setScrollbarY,
        scrollbarYEnabled,
        onScrollbarYEnabledChange: setScrollbarYEnabled,
        onCornerWidthChange: setCornerWidth,
        onCornerHeightChange: setCornerHeight,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive.div,
          {
            dir: direction,
            ...scrollAreaProps,
            ref: composedRefs,
            style: {
              position: "relative",
              // Pass corner sizes as CSS vars to reduce re-renders of context consumers
              ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
              ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
              ...props.style
            }
          }
        )
      }
    );
  }
);
ScrollArea$1.displayName = SCROLL_AREA_NAME;
var VIEWPORT_NAME = "ScrollAreaViewport";
var ScrollAreaViewport = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, children, nonce, ...viewportProps } = props;
    const context = useScrollAreaContext(VIEWPORT_NAME, __scopeScrollArea);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref, context.onViewportChange);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "style",
        {
          dangerouslySetInnerHTML: {
            __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
          },
          nonce
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          "data-radix-scroll-area-viewport": "",
          ...viewportProps,
          ref: composedRefs,
          style: {
            /**
             * We don't support `visible` because the intention is to have at least one scrollbar
             * if this component is used and `visible` will behave like `auto` in that case
             * https://developer.mozilla.org/en-US/docs/Web/CSS/overflow#description
             *
             * We don't handle `auto` because the intention is for the native implementation
             * to be hidden if using this component. We just want to ensure the node is scrollable
             * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
             * the browser from having to work out whether to render native scrollbars or not,
             * we tell it to with the intention of hiding them in CSS.
             */
            overflowX: context.scrollbarXEnabled ? "scroll" : "hidden",
            overflowY: context.scrollbarYEnabled ? "scroll" : "hidden",
            ...props.style
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: context.onContentChange, style: { minWidth: "100%", display: "table" }, children })
        }
      )
    ] });
  }
);
ScrollAreaViewport.displayName = VIEWPORT_NAME;
var SCROLLBAR_NAME = "ScrollAreaScrollbar";
var ScrollAreaScrollbar = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context;
    const isHorizontal = props.orientation === "horizontal";
    reactExports.useEffect(() => {
      isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
      return () => {
        isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
      };
    }, [isHorizontal, onScrollbarXEnabledChange, onScrollbarYEnabledChange]);
    return context.type === "hover" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarHover, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "scroll" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarScroll, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "auto" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarAuto, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "always" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarVisible, { ...scrollbarProps, ref: forwardedRef }) : null;
  }
);
ScrollAreaScrollbar.displayName = SCROLLBAR_NAME;
var ScrollAreaScrollbarHover = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [visible, setVisible] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const scrollArea = context.scrollArea;
    let hideTimer = 0;
    if (scrollArea) {
      const handlePointerEnter = () => {
        window.clearTimeout(hideTimer);
        setVisible(true);
      };
      const handlePointerLeave = () => {
        hideTimer = window.setTimeout(() => setVisible(false), context.scrollHideDelay);
      };
      scrollArea.addEventListener("pointerenter", handlePointerEnter);
      scrollArea.addEventListener("pointerleave", handlePointerLeave);
      return () => {
        window.clearTimeout(hideTimer);
        scrollArea.removeEventListener("pointerenter", handlePointerEnter);
        scrollArea.removeEventListener("pointerleave", handlePointerLeave);
      };
    }
  }, [context.scrollArea, context.scrollHideDelay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarAuto,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarScroll = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const isHorizontal = props.orientation === "horizontal";
  const debounceScrollEnd = useDebounceCallback(() => send("SCROLL_END"), 100);
  const [state, send] = useStateMachine("hidden", {
    hidden: {
      SCROLL: "scrolling"
    },
    scrolling: {
      SCROLL_END: "idle",
      POINTER_ENTER: "interacting"
    },
    interacting: {
      SCROLL: "interacting",
      POINTER_LEAVE: "idle"
    },
    idle: {
      HIDE: "hidden",
      SCROLL: "scrolling",
      POINTER_ENTER: "interacting"
    }
  });
  reactExports.useEffect(() => {
    if (state === "idle") {
      const hideTimer = window.setTimeout(() => send("HIDE"), context.scrollHideDelay);
      return () => window.clearTimeout(hideTimer);
    }
  }, [state, context.scrollHideDelay, send]);
  reactExports.useEffect(() => {
    const viewport = context.viewport;
    const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
    if (viewport) {
      let prevScrollPos = viewport[scrollDirection];
      const handleScroll = () => {
        const scrollPos = viewport[scrollDirection];
        const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
        if (hasScrollInDirectionChanged) {
          send("SCROLL");
          debounceScrollEnd();
        }
        prevScrollPos = scrollPos;
      };
      viewport.addEventListener("scroll", handleScroll);
      return () => viewport.removeEventListener("scroll", handleScroll);
    }
  }, [context.viewport, isHorizontal, send, debounceScrollEnd]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || state !== "hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": state === "hidden" ? "hidden" : "visible",
      ...scrollbarProps,
      ref: forwardedRef,
      onPointerEnter: composeEventHandlers(props.onPointerEnter, () => send("POINTER_ENTER")),
      onPointerLeave: composeEventHandlers(props.onPointerLeave, () => send("POINTER_LEAVE"))
    }
  ) });
});
var ScrollAreaScrollbarAuto = reactExports.forwardRef((props, forwardedRef) => {
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const { forceMount, ...scrollbarProps } = props;
  const [visible, setVisible] = reactExports.useState(false);
  const isHorizontal = props.orientation === "horizontal";
  const handleResize = useDebounceCallback(() => {
    if (context.viewport) {
      const isOverflowX = context.viewport.offsetWidth < context.viewport.scrollWidth;
      const isOverflowY = context.viewport.offsetHeight < context.viewport.scrollHeight;
      setVisible(isHorizontal ? isOverflowX : isOverflowY);
    }
  }, 10);
  useResizeObserver(context.viewport, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarVisible = reactExports.forwardRef((props, forwardedRef) => {
  const { orientation = "vertical", ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const thumbRef = reactExports.useRef(null);
  const pointerOffsetRef = reactExports.useRef(0);
  const [sizes, setSizes] = reactExports.useState({
    content: 0,
    viewport: 0,
    scrollbar: { size: 0, paddingStart: 0, paddingEnd: 0 }
  });
  const thumbRatio = getThumbRatio(sizes.viewport, sizes.content);
  const commonProps = {
    ...scrollbarProps,
    sizes,
    onSizesChange: setSizes,
    hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
    onThumbChange: (thumb) => thumbRef.current = thumb,
    onThumbPointerUp: () => pointerOffsetRef.current = 0,
    onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
  };
  function getScrollPosition(pointerPos, dir) {
    return getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes, dir);
  }
  if (orientation === "horizontal") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarX,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollLeft;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes, context.dir);
            thumbRef.current.style.transform = `translate3d(${offset}px, 0, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollLeft = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) {
            context.viewport.scrollLeft = getScrollPosition(pointerPos, context.dir);
          }
        }
      }
    );
  }
  if (orientation === "vertical") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarY,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollTop;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes);
            thumbRef.current.style.transform = `translate3d(0, ${offset}px, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollTop = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) context.viewport.scrollTop = getScrollPosition(pointerPos);
        }
      }
    );
  }
  return null;
});
var ScrollAreaScrollbarX = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarXChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "horizontal",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        bottom: 0,
        left: context.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
        right: context.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
        ["--radix-scroll-area-thumb-width"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollLeft + event.deltaX;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollWidth,
            viewport: context.viewport.offsetWidth,
            scrollbar: {
              size: ref.current.clientWidth,
              paddingStart: toInt(computedStyle.paddingLeft),
              paddingEnd: toInt(computedStyle.paddingRight)
            }
          });
        }
      }
    }
  );
});
var ScrollAreaScrollbarY = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarYChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "vertical",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        top: 0,
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: "var(--radix-scroll-area-corner-height)",
        ["--radix-scroll-area-thumb-height"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollTop + event.deltaY;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollHeight,
            viewport: context.viewport.offsetHeight,
            scrollbar: {
              size: ref.current.clientHeight,
              paddingStart: toInt(computedStyle.paddingTop),
              paddingEnd: toInt(computedStyle.paddingBottom)
            }
          });
        }
      }
    }
  );
});
var [ScrollbarProvider, useScrollbarContext] = createScrollAreaContext(SCROLLBAR_NAME);
var ScrollAreaScrollbarImpl = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeScrollArea,
    sizes,
    hasThumb,
    onThumbChange,
    onThumbPointerUp,
    onThumbPointerDown,
    onThumbPositionChange,
    onDragScroll,
    onWheelScroll,
    onResize,
    ...scrollbarProps
  } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, __scopeScrollArea);
  const [scrollbar, setScrollbar] = reactExports.useState(null);
  const composeRefs = useComposedRefs(forwardedRef, (node) => setScrollbar(node));
  const rectRef = reactExports.useRef(null);
  const prevWebkitUserSelectRef = reactExports.useRef("");
  const viewport = context.viewport;
  const maxScrollPos = sizes.content - sizes.viewport;
  const handleWheelScroll = useCallbackRef(onWheelScroll);
  const handleThumbPositionChange = useCallbackRef(onThumbPositionChange);
  const handleResize = useDebounceCallback(onResize, 10);
  function handleDragScroll(event) {
    if (rectRef.current) {
      const x = event.clientX - rectRef.current.left;
      const y = event.clientY - rectRef.current.top;
      onDragScroll({ x, y });
    }
  }
  reactExports.useEffect(() => {
    const handleWheel = (event) => {
      const element = event.target;
      const isScrollbarWheel = scrollbar == null ? void 0 : scrollbar.contains(element);
      if (isScrollbarWheel) handleWheelScroll(event, maxScrollPos);
    };
    document.addEventListener("wheel", handleWheel, { passive: false });
    return () => document.removeEventListener("wheel", handleWheel, { passive: false });
  }, [viewport, scrollbar, maxScrollPos, handleWheelScroll]);
  reactExports.useEffect(handleThumbPositionChange, [sizes, handleThumbPositionChange]);
  useResizeObserver(scrollbar, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollbarProvider,
    {
      scope: __scopeScrollArea,
      scrollbar,
      hasThumb,
      onThumbChange: useCallbackRef(onThumbChange),
      onThumbPointerUp: useCallbackRef(onThumbPointerUp),
      onThumbPositionChange: handleThumbPositionChange,
      onThumbPointerDown: useCallbackRef(onThumbPointerDown),
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          ...scrollbarProps,
          ref: composeRefs,
          style: { position: "absolute", ...scrollbarProps.style },
          onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
            const mainPointer = 0;
            if (event.button === mainPointer) {
              const element = event.target;
              element.setPointerCapture(event.pointerId);
              rectRef.current = scrollbar.getBoundingClientRect();
              prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
              document.body.style.webkitUserSelect = "none";
              if (context.viewport) context.viewport.style.scrollBehavior = "auto";
              handleDragScroll(event);
            }
          }),
          onPointerMove: composeEventHandlers(props.onPointerMove, handleDragScroll),
          onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
            const element = event.target;
            if (element.hasPointerCapture(event.pointerId)) {
              element.releasePointerCapture(event.pointerId);
            }
            document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
            if (context.viewport) context.viewport.style.scrollBehavior = "";
            rectRef.current = null;
          })
        }
      )
    }
  );
});
var THUMB_NAME = "ScrollAreaThumb";
var ScrollAreaThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...thumbProps } = props;
    const scrollbarContext = useScrollbarContext(THUMB_NAME, props.__scopeScrollArea);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || scrollbarContext.hasThumb, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaThumbImpl, { ref: forwardedRef, ...thumbProps }) });
  }
);
var ScrollAreaThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, style, ...thumbProps } = props;
    const scrollAreaContext = useScrollAreaContext(THUMB_NAME, __scopeScrollArea);
    const scrollbarContext = useScrollbarContext(THUMB_NAME, __scopeScrollArea);
    const { onThumbPositionChange } = scrollbarContext;
    const composedRef = useComposedRefs(
      forwardedRef,
      (node) => scrollbarContext.onThumbChange(node)
    );
    const removeUnlinkedScrollListenerRef = reactExports.useRef(void 0);
    const debounceScrollEnd = useDebounceCallback(() => {
      if (removeUnlinkedScrollListenerRef.current) {
        removeUnlinkedScrollListenerRef.current();
        removeUnlinkedScrollListenerRef.current = void 0;
      }
    }, 100);
    reactExports.useEffect(() => {
      const viewport = scrollAreaContext.viewport;
      if (viewport) {
        const handleScroll = () => {
          debounceScrollEnd();
          if (!removeUnlinkedScrollListenerRef.current) {
            const listener = addUnlinkedScrollListener(viewport, onThumbPositionChange);
            removeUnlinkedScrollListenerRef.current = listener;
            onThumbPositionChange();
          }
        };
        onThumbPositionChange();
        viewport.addEventListener("scroll", handleScroll);
        return () => viewport.removeEventListener("scroll", handleScroll);
      }
    }, [scrollAreaContext.viewport, debounceScrollEnd, onThumbPositionChange]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        "data-state": scrollbarContext.hasThumb ? "visible" : "hidden",
        ...thumbProps,
        ref: composedRef,
        style: {
          width: "var(--radix-scroll-area-thumb-width)",
          height: "var(--radix-scroll-area-thumb-height)",
          ...style
        },
        onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, (event) => {
          const thumb = event.target;
          const thumbRect = thumb.getBoundingClientRect();
          const x = event.clientX - thumbRect.left;
          const y = event.clientY - thumbRect.top;
          scrollbarContext.onThumbPointerDown({ x, y });
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, scrollbarContext.onThumbPointerUp)
      }
    );
  }
);
ScrollAreaThumb.displayName = THUMB_NAME;
var CORNER_NAME = "ScrollAreaCorner";
var ScrollAreaCorner = reactExports.forwardRef(
  (props, forwardedRef) => {
    const context = useScrollAreaContext(CORNER_NAME, props.__scopeScrollArea);
    const hasBothScrollbarsVisible = Boolean(context.scrollbarX && context.scrollbarY);
    const hasCorner = context.type !== "scroll" && hasBothScrollbarsVisible;
    return hasCorner ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaCornerImpl, { ...props, ref: forwardedRef }) : null;
  }
);
ScrollAreaCorner.displayName = CORNER_NAME;
var ScrollAreaCornerImpl = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeScrollArea, ...cornerProps } = props;
  const context = useScrollAreaContext(CORNER_NAME, __scopeScrollArea);
  const [width, setWidth] = reactExports.useState(0);
  const [height, setHeight] = reactExports.useState(0);
  const hasSize = Boolean(width && height);
  useResizeObserver(context.scrollbarX, () => {
    var _a;
    const height2 = ((_a = context.scrollbarX) == null ? void 0 : _a.offsetHeight) || 0;
    context.onCornerHeightChange(height2);
    setHeight(height2);
  });
  useResizeObserver(context.scrollbarY, () => {
    var _a;
    const width2 = ((_a = context.scrollbarY) == null ? void 0 : _a.offsetWidth) || 0;
    context.onCornerWidthChange(width2);
    setWidth(width2);
  });
  return hasSize ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.div,
    {
      ...cornerProps,
      ref: forwardedRef,
      style: {
        width,
        height,
        position: "absolute",
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: 0,
        ...props.style
      }
    }
  ) : null;
});
function toInt(value) {
  return value ? parseInt(value, 10) : 0;
}
function getThumbRatio(viewportSize, contentSize) {
  const ratio = viewportSize / contentSize;
  return isNaN(ratio) ? 0 : ratio;
}
function getThumbSize(sizes) {
  const ratio = getThumbRatio(sizes.viewport, sizes.content);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
  return Math.max(thumbSize, 18);
}
function getScrollPositionFromPointer(pointerPos, pointerOffset, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const thumbCenter = thumbSizePx / 2;
  const offset = pointerOffset || thumbCenter;
  const thumbOffsetFromEnd = thumbSizePx - offset;
  const minPointerPos = sizes.scrollbar.paddingStart + offset;
  const maxPointerPos = sizes.scrollbar.size - sizes.scrollbar.paddingEnd - thumbOffsetFromEnd;
  const maxScrollPos = sizes.content - sizes.viewport;
  const scrollRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const interpolate = linearScale([minPointerPos, maxPointerPos], scrollRange);
  return interpolate(pointerPos);
}
function getThumbOffsetFromScroll(scrollPos, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const scrollbar = sizes.scrollbar.size - scrollbarPadding;
  const maxScrollPos = sizes.content - sizes.viewport;
  const maxThumbPos = scrollbar - thumbSizePx;
  const scrollClampRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const scrollWithoutMomentum = clamp(scrollPos, scrollClampRange);
  const interpolate = linearScale([0, maxScrollPos], [0, maxThumbPos]);
  return interpolate(scrollWithoutMomentum);
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
  return scrollPos > 0 && scrollPos < maxScrollPos;
}
var addUnlinkedScrollListener = (node, handler = () => {
}) => {
  let prevPosition = { left: node.scrollLeft, top: node.scrollTop };
  let rAF = 0;
  (function loop() {
    const position = { left: node.scrollLeft, top: node.scrollTop };
    const isHorizontalScroll = prevPosition.left !== position.left;
    const isVerticalScroll = prevPosition.top !== position.top;
    if (isHorizontalScroll || isVerticalScroll) handler();
    prevPosition = position;
    rAF = window.requestAnimationFrame(loop);
  })();
  return () => window.cancelAnimationFrame(rAF);
};
function useDebounceCallback(callback, delay) {
  const handleCallback = useCallbackRef(callback);
  const debounceTimerRef = reactExports.useRef(0);
  reactExports.useEffect(() => () => window.clearTimeout(debounceTimerRef.current), []);
  return reactExports.useCallback(() => {
    window.clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = window.setTimeout(handleCallback, delay);
  }, [handleCallback, delay]);
}
function useResizeObserver(element, onResize) {
  const handleResize = useCallbackRef(onResize);
  useLayoutEffect2(() => {
    let rAF = 0;
    if (element) {
      const resizeObserver = new ResizeObserver(() => {
        cancelAnimationFrame(rAF);
        rAF = window.requestAnimationFrame(handleResize);
      });
      resizeObserver.observe(element);
      return () => {
        window.cancelAnimationFrame(rAF);
        resizeObserver.unobserve(element);
      };
    }
  }, [element, handleResize]);
}
var Root = ScrollArea$1;
var Viewport = ScrollAreaViewport;
var Corner = ScrollAreaCorner;
function ScrollArea({
  className,
  children,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Root,
    {
      "data-slot": "scroll-area",
      className: cn("relative", className),
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Viewport,
          {
            "data-slot": "scroll-area-viewport",
            className: "focus-visible:ring-ring/50 size-full rounded-[inherit] transition-[color,box-shadow] outline-none focus-visible:ring-[3px] focus-visible:outline-1",
            children
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollBar, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Corner, {})
      ]
    }
  );
}
function ScrollBar({
  className,
  orientation = "vertical",
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbar,
    {
      "data-slot": "scroll-area-scrollbar",
      orientation,
      className: cn(
        "flex touch-none p-px transition-colors select-none",
        orientation === "vertical" && "h-full w-2.5 border-l border-l-transparent",
        orientation === "horizontal" && "h-2.5 flex-col border-t border-t-transparent",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ScrollAreaThumb,
        {
          "data-slot": "scroll-area-thumb",
          className: "bg-border relative flex-1 rounded-full"
        }
      )
    }
  );
}
const PREVIEW_ROWS = 15;
function fmtDate(ms) {
  const d = new Date(ms);
  const date = d.toLocaleDateString(void 0, {
    year: "numeric",
    month: "short",
    day: "2-digit"
  });
  const time = d.toLocaleTimeString(void 0, {
    hour: "2-digit",
    minute: "2-digit",
    hour12: false
  });
  return `${date} ${time}`;
}
function fmtPrice(n) {
  return n.toLocaleString(void 0, {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
  });
}
function fmtVolume(n) {
  return Math.round(n).toLocaleString();
}
function StatCard({ icon: Icon, label, value, dataOcid }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-ocid": dataOcid,
      className: "flex items-center gap-3 rounded-md border border-border bg-card/60 px-3 py-2.5",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex size-8 shrink-0 items-center justify-center rounded-md border border-border bg-background", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "size-4 text-primary", "aria-hidden": "true" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex min-w-0 flex-col gap-0.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[11px] uppercase tracking-wider text-muted-foreground", children: label }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate font-mono text-sm font-medium text-foreground tabular-nums", children: value })
        ] })
      ]
    }
  );
}
function DataPreview({ dataset }) {
  const previewBars = dataset.bars.slice(0, PREVIEW_ROWS);
  const startDate = fmtDate(dataset.dateRange.start);
  const endDate = fmtDate(dataset.dateRange.end);
  const columnKeyByOriginal = /* @__PURE__ */ new Map();
  for (const col of dataset.columns) {
    columnKeyByOriginal.set(col.label, col.key);
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-ocid": "data_preview",
      className: "flex flex-col gap-4 rounded-lg border border-border bg-card/40 p-4",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-wrap items-center justify-between gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 min-w-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Database,
              {
                className: "size-4 shrink-0 text-primary",
                "aria-hidden": "true"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "h3",
              {
                "data-ocid": "data_preview.name",
                className: "truncate font-display text-sm font-semibold text-foreground",
                title: dataset.name,
                children: dataset.name
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Badge,
            {
              "data-ocid": "data_preview.timeframe_badge",
              variant: "secondary",
              className: "font-mono tabular-nums",
              children: [
                dataset.timeframe,
                " bars"
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            "data-ocid": "data_preview.stats",
            className: "grid grid-cols-2 gap-2 lg:grid-cols-4",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                StatCard,
                {
                  icon: Rows3,
                  label: "Rows",
                  value: dataset.rowCount.toLocaleString(),
                  dataOcid: "data_preview.stat.rows"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                StatCard,
                {
                  icon: Layers,
                  label: "Timeframe",
                  value: dataset.timeframe,
                  dataOcid: "data_preview.stat.timeframe"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                StatCard,
                {
                  icon: Calendar,
                  label: "Start",
                  value: startDate,
                  dataOcid: "data_preview.stat.start"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                StatCard,
                {
                  icon: Calendar,
                  label: "End",
                  value: endDate,
                  dataOcid: "data_preview.stat.end"
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            "data-ocid": "data_preview.columns",
            className: "rounded-md border border-border bg-background p-3",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-2 flex items-center justify-between", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs font-medium uppercase tracking-wider text-muted-foreground", children: [
                  "Detected columns (",
                  dataset.columns.length,
                  ")"
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[11px] text-muted-foreground/70", children: "Original names preserved from your header row." })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-wrap gap-1.5", children: dataset.columns.map((col, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  "data-ocid": `data_preview.column_chip.${i + 1}`,
                  className: "column-chip",
                  title: `${col.label} · ${col.type}`,
                  children: col.label
                },
                `${col.key}-${i}`
              )) })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            "data-ocid": "data_preview.table",
            className: "rounded-md border border-border bg-background",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between border-b border-border px-3 py-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs font-medium text-muted-foreground", children: [
                  "First ",
                  previewBars.length,
                  " rows"
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[11px] text-muted-foreground/70", children: [
                  "Showing a preview — all ",
                  dataset.rowCount.toLocaleString(),
                  " rows are loaded in memory."
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "h-[280px] w-full", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Table, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(TableHeader, { className: "sticky top-0 z-10 bg-background", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(TableRow, { className: "border-border hover:bg-transparent", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(TableHead, { className: "pl-3 text-xs uppercase tracking-wider text-muted-foreground", children: "#" }),
                  dataset.originalColumns.map((col, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                    TableHead,
                    {
                      className: cn(
                        "text-xs uppercase tracking-wider text-muted-foreground",
                        i > 0 && "text-right",
                        i === dataset.originalColumns.length - 1 && "pr-3"
                      ),
                      children: col
                    },
                    `${col}-${i}`
                  ))
                ] }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(TableBody, { children: previewBars.map((bar, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  TableRow,
                  {
                    "data-ocid": `data_preview.row.${i + 1}`,
                    className: "border-border/60",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(TableCell, { className: "pl-3 font-mono text-xs text-muted-foreground tabular-nums", children: i + 1 }),
                      dataset.originalColumns.map((col, ci) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                        TableCell,
                        {
                          className: cn(
                            "font-mono text-xs text-foreground tabular-nums",
                            ci > 0 && "text-right",
                            ci === dataset.originalColumns.length - 1 && "pr-3"
                          ),
                          children: renderCell(bar, col, i, dataset, columnKeyByOriginal)
                        },
                        `${col}-${ci}`
                      ))
                    ]
                  },
                  bar.timestamp
                )) })
              ] }) })
            ]
          }
        )
      ]
    }
  );
}
function renderCell(bar, col, rowIndex, dataset, columnKeyByOriginal) {
  const norm = col.trim().toLowerCase().replace(/[\s_\-]/g, "");
  if (COLUMN_ALIASES.timestamp.includes(norm) || norm === "timestamp" || norm === "time" || norm === "date" || norm === "datetime" || norm === "dt" || norm === "unix" || norm === "ts") {
    return fmtDate(bar.timestamp);
  }
  if (norm === "open" || norm === "o") return fmtPrice(bar.open);
  if (norm === "high" || norm === "h") return fmtPrice(bar.high);
  if (norm === "low" || norm === "l") return fmtPrice(bar.low);
  if (norm === "close" || norm === "c" || norm === "last" || norm === "price")
    return fmtPrice(bar.close);
  if (norm === "volume" || norm === "vol" || norm === "v")
    return fmtVolume(bar.volume);
  const cv = dataset.columnValues;
  if (cv) {
    const key = columnKeyByOriginal.get(col) ?? normalizeHeader(col);
    const arr = cv[key];
    if (arr && rowIndex < arr.length) {
      const v = arr[rowIndex];
      if (v != null && !Number.isNaN(v)) {
        return fmtPrice(v);
      }
    }
  }
  return "—";
}
function FeatureCard({ feature, enabled, onToggle }) {
  var _a;
  const isCategorical = feature.type === "categorical";
  const isCustom = feature.source === "custom";
  const [formulaOpen, setFormulaOpen] = reactExports.useState(false);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-ocid": "feature_card",
      className: cn(
        "group relative flex flex-col gap-2.5 rounded-lg border bg-card p-3 transition-smooth",
        enabled ? "border-primary/40 shadow-subtle" : "border-border opacity-70 hover:opacity-100"
      ),
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start justify-between gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex min-w-0 flex-col gap-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "font-display text-sm font-semibold leading-tight text-foreground", children: feature.name }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-wrap items-center gap-1.5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Badge,
                {
                  variant: "secondary",
                  className: "text-[10px] font-medium uppercase tracking-wide",
                  children: feature.category
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Badge,
                {
                  variant: "outline",
                  "data-ocid": `feature_card.source_badge.${feature.id}`,
                  className: cn(
                    "gap-1 text-[10px] font-medium uppercase tracking-wide",
                    isCustom ? "border-primary/40 text-primary" : "border-border text-muted-foreground"
                  ),
                  children: [
                    isCustom ? /* @__PURE__ */ jsxRuntimeExports.jsx(Upload, { className: "size-2.5", "aria-hidden": "true" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Package, { className: "size-2.5", "aria-hidden": "true" }),
                    isCustom ? "Custom" : "Built-in"
                  ]
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex shrink-0 items-center gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "span",
              {
                className: cn(
                  "text-[10px] font-medium uppercase tracking-wide tabular-nums",
                  enabled ? "text-primary" : "text-muted-foreground"
                ),
                children: enabled ? "On" : "Off"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Switch,
              {
                "data-ocid": `feature_card.toggle.${feature.id}`,
                checked: enabled,
                onCheckedChange: (checked) => onToggle(feature.id, checked),
                "aria-label": `${enabled ? "Disable" : "Enable"} ${feature.name}`
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs leading-relaxed text-muted-foreground", children: feature.description }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-auto flex flex-wrap items-center gap-1.5 pt-1", children: isCategorical ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "inline-flex items-center gap-1 rounded border border-border bg-muted/40 px-1.5 py-0.5 text-[10px] font-medium text-muted-foreground", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Hash, { className: "size-2.5", "aria-hidden": "true" }),
            "Categorical"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[10px] text-muted-foreground/70 tabular-nums", children: [
            ((_a = feature.buckets) == null ? void 0 : _a.length) ?? 0,
            " buckets"
          ] })
        ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "inline-flex items-center gap-1 rounded border border-border bg-muted/40 px-1.5 py-0.5 text-[10px] font-medium text-muted-foreground", children: [
          enabled ? /* @__PURE__ */ jsxRuntimeExports.jsx(
            ToggleRight,
            {
              className: "size-2.5 text-primary",
              "aria-hidden": "true"
            }
          ) : /* @__PURE__ */ jsxRuntimeExports.jsx(ToggleLeft, { className: "size-2.5", "aria-hidden": "true" }),
          "Numeric"
        ] }) }),
        isCategorical && feature.buckets && feature.buckets.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-wrap gap-1", children: feature.buckets.map((bucket) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          "span",
          {
            className: "rounded bg-muted/60 px-1.5 py-0.5 font-mono text-[10px] text-muted-foreground",
            children: bucket
          },
          bucket
        )) }) : null,
        !isCategorical && feature.range ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "font-mono text-[10px] text-muted-foreground/70 tabular-nums", children: [
          "range ",
          feature.range[0],
          " → ",
          feature.range[1]
        ] }) : null,
        feature.formula ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Collapsible,
          {
            open: formulaOpen,
            onOpenChange: setFormulaOpen,
            "data-ocid": `feature_card.formula.${feature.id}`,
            className: "rounded border border-border bg-muted/30",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                CollapsibleTrigger,
                {
                  "data-ocid": `feature_card.formula_trigger.${feature.id}`,
                  className: "flex w-full items-center gap-1.5 px-2 py-1.5 text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50 rounded",
                  "aria-label": `${formulaOpen ? "Hide" : "Show"} formula for ${feature.name}`,
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      ChevronRight,
                      {
                        className: cn(
                          "size-3 text-muted-foreground transition-transform",
                          formulaOpen && "rotate-90"
                        ),
                        "aria-hidden": "true"
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-semibold uppercase tracking-wide text-muted-foreground", children: "Formula" })
                  ]
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(CollapsibleContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "border-t border-border px-2 py-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className: "block font-mono text-[11px] leading-relaxed text-foreground break-words", children: feature.formula }) }) })
            ]
          }
        ) : null
      ]
    }
  );
}
const SOURCE_FILTERS = [
  { id: "all", label: "All", icon: Package },
  { id: "builtin", label: "Built-in", icon: Package },
  { id: "custom", label: "Custom", icon: Upload }
];
function FeatureCatalog({
  features,
  enabledMap,
  onToggle,
  onSetCategoryEnabled
}) {
  const [query, setQuery] = reactExports.useState("");
  const [sourceFilter, setSourceFilter] = reactExports.useState("all");
  const [openCategories, setOpenCategories] = reactExports.useState(
    () => /* @__PURE__ */ new Set()
  );
  const grouped = reactExports.useMemo(() => {
    const map = /* @__PURE__ */ new Map();
    for (const f of features) {
      const arr = map.get(f.category) ?? [];
      arr.push(f);
      map.set(f.category, arr);
    }
    return map;
  }, [features]);
  const filteredGrouped = reactExports.useMemo(() => {
    const q = query.trim().toLowerCase();
    const out = /* @__PURE__ */ new Map();
    for (const [cat, feats] of grouped) {
      const matched = feats.filter((f) => {
        if (sourceFilter === "builtin" && f.source !== "builtin") return false;
        if (sourceFilter === "custom" && f.source !== "custom") return false;
        if (!q) return true;
        return f.name.toLowerCase().includes(q) || f.description.toLowerCase().includes(q) || f.id.toLowerCase().includes(q);
      });
      if (matched.length) out.set(cat, matched);
    }
    return out;
  }, [grouped, query, sourceFilter]);
  const toggleCategory = (cat) => {
    setOpenCategories((prev) => {
      const next = new Set(prev);
      if (next.has(cat)) next.delete(cat);
      else next.add(cat);
      return next;
    });
  };
  const expandAll = () => setOpenCategories(
    new Set(grouped.keys())
  );
  const collapseAll = () => setOpenCategories(/* @__PURE__ */ new Set());
  const totalCategories = grouped.size;
  const totalMatched = Array.from(filteredGrouped.values()).reduce(
    (sum, arr) => sum + arr.length,
    0
  );
  const hasCustom = features.some((f) => f.source === "custom");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-ocid": "feature_catalog",
      className: "flex flex-col gap-3 rounded-lg border border-border bg-card/40 p-3",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative flex-1 max-w-sm", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Search,
              {
                className: "pointer-events-none absolute left-2.5 top-1/2 size-4 -translate-y-1/2 text-muted-foreground",
                "aria-hidden": "true"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Input,
              {
                "data-ocid": "feature_catalog.search_input",
                type: "search",
                placeholder: "Search features by name…",
                value: query,
                onChange: (e) => setQuery(e.target.value),
                className: "pl-9 pr-9",
                "aria-label": "Search features"
              }
            ),
            query ? /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                "data-ocid": "feature_catalog.clear_search_button",
                onClick: () => setQuery(""),
                className: "absolute right-2 top-1/2 -translate-y-1/2 rounded p-0.5 text-muted-foreground hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50",
                "aria-label": "Clear search",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "size-3.5", "aria-hidden": "true" })
              }
            ) : null
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs text-muted-foreground tabular-nums", children: [
              totalMatched,
              " of ",
              features.length,
              " features · ",
              totalCategories,
              " ",
              "categories"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                "data-ocid": "feature_catalog.expand_all_button",
                variant: "ghost",
                size: "sm",
                onClick: expandAll,
                children: "Expand all"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                "data-ocid": "feature_catalog.collapse_all_button",
                variant: "ghost",
                size: "sm",
                onClick: collapseAll,
                children: "Collapse all"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "fieldset",
          {
            className: "flex items-center gap-1.5 border-0 p-0 m-0",
            "aria-label": "Filter features by source",
            children: SOURCE_FILTERS.map((opt) => {
              if (opt.id === "custom" && !hasCustom) return null;
              const Icon = opt.icon;
              const active = sourceFilter === opt.id;
              return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "button",
                {
                  type: "button",
                  "data-ocid": `feature_catalog.source_filter.${opt.id}`,
                  "aria-pressed": active,
                  onClick: () => setSourceFilter(opt.id),
                  className: cn(
                    "inline-flex items-center gap-1.5 rounded-md border px-2.5 py-1 text-xs font-medium transition-smooth focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50",
                    active ? "border-primary/40 bg-primary/10 text-primary" : "border-border bg-card text-muted-foreground hover:text-foreground"
                  ),
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "size-3", "aria-hidden": "true" }),
                    opt.label
                  ]
                },
                opt.id
              );
            })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "max-h-[640px]", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-2 pr-2", children: [
          filteredGrouped.size === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "rounded-md border border-dashed border-border px-4 py-8 text-center text-sm text-muted-foreground", children: query ? `No features match "${query}"` : sourceFilter === "custom" ? "No custom (uploaded-column) features available. Upload a dataset with non-OHLCV columns to generate custom features." : sourceFilter === "builtin" ? "No built-in features available." : "No features available." }) : null,
          Array.from(filteredGrouped.entries()).map(([category, feats]) => {
            const isOpen = openCategories.has(category);
            const enabledCount = feats.filter(
              (f) => enabledMap[f.id] ?? f.enabled
            ).length;
            const allEnabled = enabledCount === feats.length;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Collapsible,
              {
                open: isOpen,
                onOpenChange: () => toggleCategory(category),
                "data-ocid": `feature_catalog.category.${category.toLowerCase().replace(/\s+/g, "_")}`,
                className: "rounded-md border border-border bg-card",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-2 px-3 py-2.5", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      CollapsibleTrigger,
                      {
                        "data-ocid": `feature_catalog.category_trigger.${category.toLowerCase().replace(/\s+/g, "_")}`,
                        className: "flex flex-1 items-center gap-2 text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50 rounded",
                        children: [
                          isOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                            ChevronDown,
                            {
                              className: "size-4 text-muted-foreground",
                              "aria-hidden": "true"
                            }
                          ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
                            ChevronRight,
                            {
                              className: "size-4 text-muted-foreground",
                              "aria-hidden": "true"
                            }
                          ),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-display text-sm font-semibold text-foreground", children: category }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "rounded bg-muted/60 px-1.5 py-0.5 font-mono text-[10px] text-muted-foreground tabular-nums", children: feats.length }),
                          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[10px] text-muted-foreground/70 tabular-nums", children: [
                            enabledCount,
                            "/",
                            feats.length,
                            " on"
                          ] })
                        ]
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      Button,
                      {
                        "data-ocid": `feature_catalog.${allEnabled ? "deselect_all" : "select_all"}.${category.toLowerCase().replace(/\s+/g, "_")}`,
                        variant: "ghost",
                        size: "sm",
                        className: "h-7 px-2 text-xs",
                        onClick: () => onSetCategoryEnabled(category, !allEnabled),
                        children: allEnabled ? "Deselect all" : "Select all"
                      }
                    )
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(CollapsibleContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "div",
                    {
                      className: cn(
                        "grid grid-cols-1 gap-2 border-t border-border px-3 py-3",
                        "sm:grid-cols-2 lg:grid-cols-3"
                      ),
                      children: feats.map((f) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                        FeatureCard,
                        {
                          feature: f,
                          enabled: enabledMap[f.id] ?? f.enabled,
                          onToggle
                        },
                        f.id
                      ))
                    }
                  ) })
                ]
              },
              category
            );
          })
        ] }) })
      ]
    }
  );
}
const CHART_COLORS = [
  "oklch(var(--chart-1))",
  "oklch(var(--chart-2))",
  "oklch(var(--chart-3))",
  "oklch(var(--chart-4))",
  "oklch(var(--chart-5))"
];
function fmt(n, digits = 2) {
  if (!Number.isFinite(n)) return "—";
  const abs = Math.abs(n);
  if (abs >= 1e3)
    return n.toLocaleString(void 0, { maximumFractionDigits: 0 });
  if (abs >= 100) return n.toFixed(1);
  return n.toFixed(digits);
}
function computeNumericStats(values) {
  const nums = values.filter(
    (v) => typeof v === "number" && Number.isFinite(v)
  );
  if (nums.length === 0) return null;
  let min = Number.POSITIVE_INFINITY;
  let max = Number.NEGATIVE_INFINITY;
  let sum = 0;
  for (const n of nums) {
    if (n < min) min = n;
    if (n > max) max = n;
    sum += n;
  }
  const mean = sum / nums.length;
  const sorted = nums.slice().sort((a, b) => a - b);
  const pct = (p) => {
    const idx = Math.min(
      sorted.length - 1,
      Math.floor(p / 100 * sorted.length)
    );
    return sorted[idx] ?? mean;
  };
  return {
    min,
    max,
    mean,
    p25: pct(25),
    p50: pct(50),
    p75: pct(75),
    count: nums.length
  };
}
function computeCategoricalStats(values, buckets) {
  const counts = /* @__PURE__ */ new Map();
  for (const b of buckets) counts.set(b, 0);
  let total = 0;
  for (const v of values) {
    if (typeof v === "string" && counts.has(v)) {
      counts.set(v, (counts.get(v) ?? 0) + 1);
      total++;
    }
  }
  return buckets.map((bucket) => ({
    bucket,
    count: counts.get(bucket) ?? 0,
    pct: total > 0 ? (counts.get(bucket) ?? 0) / total * 100 : 0
  }));
}
function FeatureStats({
  features,
  featureValues,
  enabledMap
}) {
  const stats = reactExports.useMemo(() => {
    return features.filter((f) => enabledMap[f.id] ?? f.enabled).map((f) => {
      const values = featureValues[f.id] ?? [];
      if (f.type === "categorical" && f.buckets) {
        return {
          feature: f,
          kind: "categorical",
          distribution: computeCategoricalStats(values, f.buckets)
        };
      }
      return {
        feature: f,
        kind: "numeric",
        stats: computeNumericStats(values)
      };
    });
  }, [features, featureValues, enabledMap]);
  if (stats.length === 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        "data-ocid": "feature_stats.empty_state",
        className: "rounded-lg border border-dashed border-border bg-card/40 px-4 py-8 text-center text-sm text-muted-foreground",
        children: "No features enabled. Toggle features on in the catalog above to see their stats."
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      "data-ocid": "feature_stats",
      className: "grid grid-cols-1 gap-3 lg:grid-cols-2",
      children: stats.map(({ feature, kind }) => {
        const isCategorical = kind === "categorical";
        const numericStats = kind === "numeric" ? kind.stats : null;
        const distribution = kind === "categorical" ? kind.distribution : null;
        return /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            "data-ocid": `feature_stats.card.${feature.id}`,
            className: "flex flex-col gap-3 rounded-lg border border-border bg-card p-4",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start justify-between gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex min-w-0 flex-col gap-1", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "font-display text-sm font-semibold text-foreground", children: feature.name }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    Badge,
                    {
                      variant: "secondary",
                      className: "w-fit text-[10px] uppercase tracking-wide",
                      children: feature.category
                    }
                  )
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "span",
                  {
                    className: cn(
                      "inline-flex items-center gap-1 rounded border px-1.5 py-0.5 text-[10px] font-medium",
                      isCategorical ? "border-border bg-muted/40 text-muted-foreground" : "border-primary/30 bg-primary/10 text-primary"
                    ),
                    children: [
                      isCategorical ? /* @__PURE__ */ jsxRuntimeExports.jsx(Hash, { className: "size-2.5", "aria-hidden": "true" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Sigma, { className: "size-2.5", "aria-hidden": "true" }),
                      isCategorical ? "Categorical" : "Numeric"
                    ]
                  }
                )
              ] }),
              isCategorical && distribution ? /* @__PURE__ */ jsxRuntimeExports.jsx(CategoricalDistribution, { distribution }) : /* @__PURE__ */ jsxRuntimeExports.jsx(NumericDistribution, { stats: numericStats })
            ]
          },
          feature.id
        );
      })
    }
  );
}
function CategoricalDistribution({
  distribution
}) {
  const chartData = distribution.map((d) => ({
    bucket: d.bucket,
    count: d.count,
    pct: Number(d.pct.toFixed(1))
  }));
  let maxCount = 1;
  for (let i = 0; i < distribution.length; i++) {
    if (distribution[i].count > maxCount) maxCount = distribution[i].count;
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-32 w-full", "data-ocid": "feature_stats.chart", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ResponsiveContainer, { width: "100%", height: "100%", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
      BarChart,
      {
        data: chartData,
        margin: { top: 4, right: 4, bottom: 0, left: -20 },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            XAxis,
            {
              dataKey: "bucket",
              tick: { fontSize: 9, fill: "oklch(var(--muted-foreground))" },
              tickLine: false,
              axisLine: { stroke: "oklch(var(--border))" },
              interval: 0,
              angle: -30,
              textAnchor: "end",
              height: 36
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            YAxis,
            {
              tick: { fontSize: 9, fill: "oklch(var(--muted-foreground))" },
              tickLine: false,
              axisLine: false,
              width: 32
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Tooltip,
            {
              cursor: { fill: "oklch(var(--muted) / 0.4)" },
              contentStyle: {
                background: "oklch(var(--popover))",
                border: "1px solid oklch(var(--border))",
                borderRadius: "6px",
                fontSize: "11px",
                color: "oklch(var(--popover-foreground))"
              },
              formatter: (value) => [`${value} bars`, "Count"]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Bar, { dataKey: "count", radius: [2, 2, 0, 0], children: chartData.map((d, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            Cell,
            {
              fill: CHART_COLORS[i % CHART_COLORS.length]
            },
            d.bucket
          )) })
        ]
      }
    ) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col gap-1", children: distribution.map((d) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        "data-ocid": "feature_stats.bucket_row",
        className: "flex items-center gap-2",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "w-20 shrink-0 truncate font-mono text-[10px] text-muted-foreground", children: d.bucket }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "relative h-1.5 flex-1 overflow-hidden rounded-full bg-muted/60", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "absolute left-0 top-0 h-full rounded-full bg-primary transition-all",
              style: { width: `${d.count / maxCount * 100}%` }
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "w-10 shrink-0 text-right font-mono text-[10px] text-foreground tabular-nums", children: d.count.toLocaleString() }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "w-12 shrink-0 text-right font-mono text-[10px] text-muted-foreground tabular-nums", children: [
            d.pct.toFixed(1),
            "%"
          ] })
        ]
      },
      d.bucket
    )) })
  ] });
}
function NumericDistribution({ stats }) {
  if (!stats) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "rounded-md border border-dashed border-border px-3 py-4 text-center text-xs text-muted-foreground", children: "No values computed yet." });
  }
  const cells = [
    { label: "Min", value: stats.min, icon: Minus },
    { label: "Mean", value: stats.mean, icon: Sigma },
    { label: "Max", value: stats.max, icon: Plus },
    { label: "P25", value: stats.p25, icon: Minus },
    { label: "P50", value: stats.p50, icon: Sigma },
    { label: "P75", value: stats.p75, icon: Plus }
  ];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-3 gap-2", children: cells.map(({ label, value, icon: Icon }) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        "data-ocid": `feature_stats.stat.${label.toLowerCase()}`,
        className: "flex flex-col gap-0.5 rounded-md border border-border bg-muted/30 px-2 py-1.5",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "flex items-center gap-1 text-[10px] uppercase tracking-wide text-muted-foreground", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "size-2.5", "aria-hidden": "true" }),
            label
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono text-xs font-semibold text-foreground tabular-nums", children: fmt(value) })
        ]
      },
      label
    )) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between rounded-md border border-border bg-muted/20 px-2.5 py-1.5", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] uppercase tracking-wide text-muted-foreground", children: "Sample size" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "font-mono text-xs font-semibold text-primary tabular-nums", children: [
        stats.count.toLocaleString(),
        " bars"
      ] })
    ] })
  ] });
}
function FeatureGeneratorPage() {
  const dataset = useEngineStore((s) => s.dataset);
  const features = useEngineStore((s) => s.features);
  const featureValues = useEngineStore((s) => s.featureValues);
  const isComputing = useEngineStore((s) => s.isComputing);
  const completedSteps = useEngineStore((s) => s.completedSteps);
  const generateFeaturesAction = useEngineStore(
    (s) => s.generateFeaturesAction
  );
  const featuresGenerated = completedSteps.has("featuresGenerated");
  const [enabledMap, setEnabledMap] = reactExports.useState({});
  const effectiveEnabledMap = reactExports.useMemo(() => {
    const out = { ...enabledMap };
    for (const f of features) {
      if (!(f.id in out)) out[f.id] = f.enabled;
    }
    return out;
  }, [features, enabledMap]);
  const handleToggle = (featureId, enabled) => {
    setEnabledMap((prev) => ({ ...prev, [featureId]: enabled }));
  };
  const handleSetCategoryEnabled = (category, enabled) => {
    setEnabledMap((prev) => {
      const next = { ...prev };
      for (const f of features) {
        if (f.category === category) next[f.id] = enabled;
      }
      return next;
    });
  };
  if (!dataset) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-ocid": "page.feature_generator", className: "px-4 py-8 md:px-6", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      EmptyState,
      {
        icon: Database,
        title: "No data loaded yet",
        description: "Load a CSV of OHLCV bars (or use the built-in sample dataset) to start generating features. The engine derives hundreds of measurable characteristics from each bar — candle shape, VWAP distance, time of day, volume behavior, and more.",
        hint: "Tip: click “Use Sample” in the header to load one year of realistic intraday data instantly."
      }
    ) });
  }
  if (isComputing && !featuresGenerated) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        "data-ocid": "page.feature_generator.loading_state",
        className: "flex flex-col items-center justify-center gap-6 px-4 py-20 md:px-6",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex size-16 items-center justify-center rounded-full border border-primary/30 bg-primary/10", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Sparkles,
            {
              className: "size-7 animate-pulse-soft text-primary",
              "aria-hidden": "true"
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex max-w-md flex-col items-center gap-3 text-center", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-display text-lg font-semibold text-foreground", children: "Generating features…" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-sm text-muted-foreground", children: [
              "Deriving hundreds of measurable characteristics from",
              " ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono text-foreground tabular-nums", children: dataset.rowCount.toLocaleString() }),
              " ",
              "bars. This runs entirely in your browser."
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full max-w-sm", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Progress,
                {
                  "data-ocid": "page.feature_generator.progress",
                  value: 62,
                  className: "h-1.5"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-2 flex items-center justify-between text-[10px] uppercase tracking-wide text-muted-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Computing" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono tabular-nums", children: "in progress" })
              ] })
            ] })
          ] })
        ]
      }
    );
  }
  if (!featuresGenerated || features.length === 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-ocid": "page.feature_generator", className: "px-4 py-8 md:px-6", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mx-auto flex max-w-2xl flex-col items-center gap-6 py-12 text-center", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex size-16 items-center justify-center rounded-full border border-primary/30 bg-primary/10", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Zap, { className: "size-7 text-primary", "aria-hidden": "true" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-display text-xl font-semibold text-foreground", children: "Generate features from your data" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-sm leading-relaxed text-muted-foreground", children: [
          "The engine will derive hundreds of measurable features from your",
          " ",
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono text-foreground tabular-nums", children: dataset.rowCount.toLocaleString() }),
          " ",
          "bars across 11 categories — candle structure, VWAP, time, calendar, volume, volatility, location, gaps, opening range, Bollinger bands, and trend. Each feature gets a plain-English description so you know exactly what it measures."
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          "data-ocid": "page.feature_generator.generate_button",
          size: "lg",
          onClick: generateFeaturesAction,
          disabled: isComputing,
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "size-4", "aria-hidden": "true" }),
            "Generate Features"
          ]
        }
      )
    ] }) });
  }
  const categories = new Set(features.map((f) => f.category));
  const enabledCount = features.filter(
    (f) => effectiveEnabledMap[f.id] ?? f.enabled
  ).length;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-ocid": "page.feature_generator",
      className: "flex flex-col gap-6 px-4 py-6 md:px-6",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "section",
          {
            "data-ocid": "page.feature_generator.data_preview_section",
            className: "flex flex-col gap-2",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Database, { className: "size-4 text-primary", "aria-hidden": "true" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-display text-sm font-semibold uppercase tracking-wide text-foreground", children: "Loaded Dataset" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: "Preview the first rows of your dataset with original column names preserved. Custom indicator columns (non-OHLCV) are shown alongside OHLCV fields." }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(DataPreview, { dataset })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3 rounded-lg border border-border bg-card p-4 shadow-subtle sm:flex-row sm:items-center sm:justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex size-10 items-center justify-center rounded-md border border-primary/30 bg-primary/10", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ListTree, { className: "size-5 text-primary", "aria-hidden": "true" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("h2", { className: "font-display text-base font-semibold text-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono tabular-nums text-primary", children: features.length }),
                " ",
                "features generated across",
                " ",
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono tabular-nums text-primary", children: categories.size }),
                " ",
                "categories"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono tabular-nums", children: enabledCount }),
                " of",
                " ",
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono tabular-nums", children: features.length }),
                " ",
                "enabled for pattern testing ·",
                " ",
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono", children: dataset.name })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              "data-ocid": "page.feature_generator.regenerate_button",
              variant: "outline",
              size: "sm",
              onClick: generateFeaturesAction,
              disabled: isComputing,
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "size-4", "aria-hidden": "true" }),
                "Regenerate"
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "section",
          {
            "data-ocid": "page.feature_generator.catalog_section",
            className: "flex flex-col gap-2",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ListTree, { className: "size-4 text-primary", "aria-hidden": "true" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-display text-sm font-semibold uppercase tracking-wide text-foreground", children: "Feature Catalog" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: "Browse every generated feature by category — this is the feature glossary. Each card shows a plain-English definition, a source badge (Built-in vs Custom for uploaded columns), and an expandable Formula section with the exact computation. Toggle features on or off to control which ones the discovery engine will test, filter by source, or use search to find a specific feature by name." }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                FeatureCatalog,
                {
                  features,
                  enabledMap: effectiveEnabledMap,
                  onToggle: handleToggle,
                  onSetCategoryEnabled: handleSetCategoryEnabled
                }
              )
            ]
          }
        ),
        featureValues ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "section",
          {
            "data-ocid": "page.feature_generator.stats_section",
            className: "flex flex-col gap-2",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ChartColumn, { className: "size-4 text-primary", "aria-hidden": "true" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-display text-sm font-semibold uppercase tracking-wide text-foreground", children: "Summary Statistics" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: "Understand the shape of your data. Numeric features show min, max, mean, and percentile distribution. Categorical features show how often each bucket occurs." }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                FeatureStats,
                {
                  features,
                  featureValues,
                  enabledMap: effectiveEnabledMap
                }
              )
            ]
          }
        ) : null
      ]
    }
  );
}
export {
  FeatureGeneratorPage as default
};
